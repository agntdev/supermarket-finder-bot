{
  "name": "supermarket-finder-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram bot that finds nearby supermarkets worldwide using OpenStreetMap",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "grammy": "^1.34.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
